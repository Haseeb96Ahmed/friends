
<h1>Friends List</h1>

<table class="table table-striped  table-bordered  table-hover">
  <br/>
  <thead class="table-dark">
    <tr>
      <th >Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>Twitter</th>
      <th>User ID</th>
      <th ></th>
    </tr>
  </thead>

  <tbody>
    <% @friends.each do |friend| %>
      <%#if friend.user == current_user -----------> if you want to show each user his/her own friendslist %>
      <tr>
        <td>
<%= link_to friend.first_name + ' ' + friend.last_name ,friend %>
             
          </td>
        <td><%= friend.phone %></td>
        <td><%= friend.email %></td>
        <td><%= friend.twitter %></td>
        <td><%= friend.user_id %></td>
        
        
        <td><%= link_to 'Delete', friend,class:"btn btn-primary  btn-sml", method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
      <%# end  -----------> if you want to show each user his/her own friendslist %>
    <% end %>
  </tbody>
</table>

<br/>

<%= link_to 'New Friend', new_friend_path, class:"btn btn-success btn-lg" %><br/><br/>
